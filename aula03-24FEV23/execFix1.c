#include <stdio.h>

int main(){
   int inicialVector[8] = {0};
   int positiveVector[8] = {0};
   int negativeVector[8] = {0};
   int i = 0;
   int VECTOR_LENGHT = 8;
   for ( i = 0; i < VECTOR_LENGHT; i++)
   {
      printf('Digite o valor da posição %d',i);
      scanf('%d', &inicialVector[i]);
   }
   for ( i = 0; i < VECTOR_LENGHT; i++)
   {
      if (inicialVector[i] >= 0)
      {
         
      }
      
   }

   
}